// import { CartItem } from "../types/cartItems";
// import { useProducts } from "../context/ProductContext";
// const transformToC = (apiCart: []) => {
//   const { products } = useProducts();

// const cartItem: [] = apiCart.map((item: any) => ({
//     id: item.productId,
//     title: products.find(p => p._id === item.productId)?.title || '',
//     price: products.find(p => p._id === item.productId)?.price || '',
//     image: products.find(p => p._id === item.productId)?.image || '',
//     quantity: item.quantity
//   }));

// }

//   export default transformToC;